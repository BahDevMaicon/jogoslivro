{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Rodar no Chrome (npm run dev)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "dev"
    }
  ]
}
