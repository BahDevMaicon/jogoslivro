# Copie este arquivo para ".env" e preencha com os valores do seu projeto Supabase
# (painel do projeto em supabase.com → Project Settings → API).
# Nunca coloque a "service_role key" aqui — só a "anon/public key".

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
